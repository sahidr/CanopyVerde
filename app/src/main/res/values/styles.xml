<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorCanopy</item>
        <item name="colorPrimaryDark">@color/colorCanopyDark</item>
        <item name="colorAccent">@color/colorCanopy</item>
        <item name="android:textColorPrimary">@color/colorCanopyLight</item>
        <item name="android:colorForeground">@color/colorCanopyLight</item>
        <item name="android:textColorPrimaryInverse">?android:attr/textColorPrimaryNoDisable</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="CanopyVerde" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:navigationBarColor">@color/colorCanopy</item>
        <item name="android:statusBarColor">@color/colorCanopy</item>
        <item name="android:colorForeground">@android:color/white</item>
        <item name="android:textColorPrimary">@color/fontColor</item>
        <item name="colorPrimary">@color/colorCanopy</item>
        <item name="colorAccent">@color/colorCanopy</item>
        <item name="colorPrimaryDark">@color/colorCanopyDark</item>
        <item name="android:textAppearance">@style/TextAppearance.FontPath</item>
        <item name="android:textColor">@color/fontColor</item>
        <item name="android:colorBackground">@android:color/white</item>
        <item name="android:textColorHint">@color/black_overlay</item>
        <item name="android:spinnerItemStyle">@style/spinnerItemStyle</item>
    </style>

    <style name="spinnerItemStyle">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/black_overlay</item>
        <item name="android:textAppearance">@style/TextAppearance.FontPath</item>
    </style>

    <style name="TextAppearance.FontPath" parent="android:TextAppearance">
        <item name="fontPath">fonts/TitilliumWeb-Regular.ttf</item>
    </style>

    <style name="CanopyVerde.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="CanopyVerde.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
